<!DOCTYPE html>
{% haml %}
%html lang="en"

  {{ head() }}
    %title << {{ title }}

  %body#wrapper onload="come_alert()"
    {{ header() }}

    #content
      -block content

    {{ footer() }}

{% endhaml %}
